A Server -> B Server or B Server -> A Server

# configration both A and B
$ ssh-keygen -t rsa
$ ssh-copy-id -i ~/.ssh/id_rsa.pub [user]@[host]
$ chmod 700 ~/.ssh/
$ chmod 600 ~/.ssh/authorized_keys

# if using selinux
$ restorecon -R -v ~/.ssh
